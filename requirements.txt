# Core dependencies for IDM curiosity-driven RL in Doom
# Requires Python 3.12 (PyTorch compatible)

# Deep Learning
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Numerics and utilities
numpy==1.26.4
pillow==10.4.0

# Experiment tracking
wandb==0.22.2

# ViZDoom - Doom environment for RL
# Note: May need to be built from source for best compatibility
# If pip install fails, use: git clone https://github.com/Farama-Foundation/ViZDoom.git && cd ViZDoom && python -m pip install .
vizdoom
